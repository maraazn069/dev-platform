events {
    worker_connections 1024;
}

http {
    # Redirect semua HTTP ke HTTPS
    server {
        listen 80;
        server_name _;
        return 301 https://$host$request_uri;
    }

    # Template konfigurasi per-user (digunakan oleh add-user.sh)
    # Contoh untuk user1: user1.dev.domainku.com
    # Setiap user mendapat instance code-server sendiri

    # Konfigurasi upstream untuk tiap user
    # Format: upstream codeserver_USERNAME { server 127.0.0.1:PORT; }
    # Port mulai dari 8081, 8082, 8083, dst
    # ---- USER CONFIGS BELOW (auto-generated oleh add-user.sh) ----

    # ---- END USER CONFIGS ----

    # Contoh konfigurasi untuk user pertama (user1)
    # Uncomment dan sesuaikan setelah menjalankan add-user.sh

    # upstream codeserver_user1 {
    #     server 127.0.0.1:8081;
    # }

    # server {
    #     listen 443 ssl;
    #     server_name user1.dev.domainku.com;
    #
    #     ssl_certificate /etc/letsencrypt/live/user1.dev.domainku.com/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/user1.dev.domainku.com/privkey.pem;
    #
    #     location / {
    #         proxy_pass http://codeserver_user1;
    #         proxy_set_header Host $host;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection upgrade;
    #         proxy_set_header Accept-Encoding gzip;
    #     }
    # }
}
